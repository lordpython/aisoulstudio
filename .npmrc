# Use legacy peer deps to resolve conflicts
# This is needed because @langchain/community has a transitive dependency
# (@browserbasehq/stagehand) that requires dotenv@^16.4.5, but we use dotenv@17.2.3
# dotenv 17 is backward compatible with 16, so this is safe
legacy-peer-deps=true
